# Copy this file to .env and fill in real values. .env is gitignored — never commit it.

# Required for the AI Summary feature (see ai_summary.py). Without this,
# the dashboard still runs; the AI Summary card just shows a warning instead.
OPENAI_API_KEY=
